/*
mod utils;

use utils::add;
#[test]
fn it_works() {
    let result = add(2, 2);
    assert_eq!(result, 4);
}

#[test]
fn it_works2() {
    let result = add(2, 2);
    assert_eq!(result, 4);
}

use testsi::fixtures::*;
use testsi::rstest::*;

#[rstest]
fn it_works3(two: usize) {
    assert_eq!(two, 2);
}
*/
